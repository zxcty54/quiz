name: Scrape Bihar Tourism Events

on:
  schedule:
    # Daily subah 6:00 AM IST (00:30 UTC) par chalega
    - cron: '30 0 * * *'
  workflow_dispatch: # Manual trigger GitHub UI se

jobs:
  scrape-bihar-events:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Output JSON commit aur push karne ke liye zaroori hai

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 urllib3

      - name: Run Bihar Tourism Scraper
        run: |
          python bihar_events_scraper.py

      - name: Commit and Push Events JSON
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add bihar_events_2026_2027.json
          
          if git diff --staged --quiet; then
            echo "Koi naya event update nahi mila. Commit skipped."
          else
            git commit -m "📅 Auto-update Bihar Tourism Events: $(date +'%d-%b-%Y')"
            git pull --rebase origin main
            git push origin main
          fi
